<div>
  <h3><%= current_user.name %></h3>
      <p>name <%= image_tag current_user.get_profile_image(100,100) %></p>
      <p>introduction <%= current_user.introduction %></p>
</div>

<h1>New Book</h1>
  <%= form_with model: @book do |f| %>

    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create book' %>
  <% end %>


  <h1>Users</h1>
 <p>image</p>
  <p>name</p>
  <% @users.each do |user| %>

  <p><%= image_tag user.get_profile_image(100,100) %></p>


  <p><%= user.name %></p>

  <%= link_to "Show", user_path(user)%>
  </div>
<% end %>